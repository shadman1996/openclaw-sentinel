# Web subsystem
flask==3.0.3
werkzeug==3.0.3

# Core telemetry
psutil>=6.0.0

# Desktop UI Wrapper (Optional)
# Uncomment if building native WinForms/Edge app (Requires Python <= 3.12 for pythonnet)
# pywebview>=5.1.0

# NOTE: The v2.1 Advanced OS Optimization features utilize pure Python standard libraries 
# (winreg, ctypes, subprocess, shutil) and require zero additional pip packages!
